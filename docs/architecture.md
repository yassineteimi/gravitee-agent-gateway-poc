# Architecture

The components, the request flow through each gate, and the GitOps control loop.

!!! info "In progress"
    Filled in once the platform is running. Covers Gravitee's components (Gateway, Management API, Console UI, Developer Portal, MongoDB, Elasticsearch), the three-gate request flow, the ArgoCD app-of-apps control loop, and the homelab topology.
