# Bootstrap (M0)

Stand up the Kubernetes cluster, install ArgoCD, and deploy Gravitee APIM via Helm, all GitOps-first.

!!! info "In progress"
    Written during **Milestone 0**. Creates the local cluster, installs ArgoCD and the app-of-apps, deploys the pinned Gravitee Helm chart, injects the Enterprise license, and verifies every component is healthy.
