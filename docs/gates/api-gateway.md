# Gate 1: API Gateway

Identity and abuse control on a sample API: a Plan with authentication, rate limiting and quota, published for self-service through the Developer Portal.

!!! info "In progress"
    Written during **Milestone 1**. Publishes a v4 proxy API, secures it with a Plan (API key / JWT / OAuth2), applies rate-limit and quota policies, and demonstrates allowed versus throttled versus rejected traffic, all reconciled by ArgoCD.
